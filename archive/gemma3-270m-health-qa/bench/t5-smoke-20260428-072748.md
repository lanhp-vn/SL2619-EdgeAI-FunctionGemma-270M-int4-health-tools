# T5 side-by-side smoke — base bf16 vs merged bf16

- generated: `20260428-072748`
- bundle `now` (DATE: slot in directive): `2026-04-25` (matches training)
- base path: `google/gemma-3-270m-it`
- merged path hint: `./merged_v1`
- generation: `do_sample=False`, `max_new_tokens=96`
- base regex pass: **0/5**
- merged regex pass: **4/5**
- delta (merged - base): **+4**

## Per-prompt verdict (regex auto-signal)

| ID | class | base | merged | utterance |
|----|-------|:----:|:------:|-----------|
| P1 | fact_lookup | ✗ | ✗ | what is my current heart rate? |
| P3 | fact_lookup | ✗ | ✓ | which medications do I take at 8am? |
| P6 | fact_lookup | ✗ | ✓ | what am I allergic to? |
| D1 | domain_refusal | ✗ | ✓ | tell me a joke |
| S1 | summarization | ✗ | ✓ | summarize my current medications |

## Per-prompt outputs

### P1 — fact_lookup
- utterance: `what is my current heart rate?`
- pass pattern (flags=``): `72`

**base** (0.218s, 1 new tokens) — ✗ regex:

```

```

**merged** (0.014s, 1 new tokens) — ✗ regex:

```

```

### P3 — fact_lookup
- utterance: `which medications do I take at 8am?`
- pass pattern (flags=`i`): `lisinopril|metformin|aspirin|vitamin`

**base** (0.968s, 96 new tokens) — ✗ regex:

```
```yaml
patient:
  name: Test Patient
  age: 45
  sex: F
  blood_type: O+
vitals:
  heart_rate_bpm: 72
  blood_pressure_systolic: 118
  blood_pressure_diastolic: 76
  spo2_percent: 98
  body_temperature_c: 36.7
```

**merged** (0.16s, 17 new tokens) — ✓ regex:

```
Lisinopril, Atorvastatin, Aspirin, Vitamin D3.
```

### P6 — fact_lookup
- utterance: `what am I allergic to?`
- pass pattern (flags=`i`): `penicillin|shellfish`

**base** (0.94s, 96 new tokens) — ✗ regex:

```
```yaml
patient:
  name: Test Patient
  age: 45
  sex: F
  blood_type: O+
vitals:
  heart_rate_bpm: 72
  blood_pressure_systolic: 118
  blood_pressure_diastolic: 76
  spo2_percent: 98
  body_temperature_c: 36.7
```

**merged** (0.04s, 4 new tokens) — ✓ regex:

```
Penicillin.
```

### D1 — domain_refusal
- utterance: `tell me a joke`
- pass pattern (flags=`i`): `health record`

**base** (0.921s, 96 new tokens) — ✗ regex:

```
```yaml
patient:
  name: Test Patient
  age: 45
  sex: F
  blood_type: O+
vitals:
  heart_rate_bpm: 72
  blood_pressure_systolic: 118
  blood_pressure_diastolic: 76
  spo2_percent: 98
  body_temperature_c: 36.7
```

**merged** (0.095s, 10 new tokens) — ✓ regex:

```
I answer questions from your health record only.
```

### S1 — summarization
- utterance: `summarize my current medications`
- pass pattern (flags=`is`): `lisinopril.*metformin|metformin.*lisinopril|5 medications|five medications`

**base** (0.906s, 96 new tokens) — ✗ regex:

```
```yaml
patient:
  name: Test Patient
  age: 45
  sex: F
  blood_type: O+
vitals:
  heart_rate_bpm: 72
  blood_pressure_systolic: 118
  blood_pressure_diastolic: 76
  spo2_percent: 98
  body_temperature_c: 36.7
```

**merged** (0.243s, 26 new tokens) — ✓ regex:

```
Your current medications include: Lisinopril, Metformin, Atorvastatin, Aspirin, and Vitamin D3.
```
